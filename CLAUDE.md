# CLAUDE.md

## 本次对话中的错误记录与反思

### 2026-07-01: 方案I (Deep Supervision) 误判

**错误行为**：
用户展示了方案I epoch 0 mAP = 14.4% 的结果。我建议"继续跑更多 epoch 来观察训练策略改进的效果"。

**为什么这是错的**：
RT-DETR 框架**默认已有完整的 aux loss**（每层 decoder 都有 aux head，权重为 1.0）。方案I 唯一做的改动是把浅层 aux loss 权重从 1.0 降至 0.3 和 0.6。这不是"引入新机制等待生效"，而是"削弱已有的训练信号"。理论上就不应该期待它带来改善。

**根本原因**：
1. 没有把"框架已有机制"和"新增改动"区分开来。看到 aux loss 就当成新东西，没有注意到 baseline 日志里早就有了 `loss_vfl_aux_0`、`loss_vfl_aux_1`
2. 用模板式判断（"训练策略需要多 epoch"）替代了针对性分析
3. 在用户直接指出问题后还在辩解，没有立刻承认错误

**今后规则**：
- 看到实验结果为 baseline 值时，第一反应应该是检查"改动能不能从原理上讲通"，而不是默认"需要更多时间"
- 对于 loss 系数调整类改动：如果只是缩小/放大已有信号，epoch 0 就能判断——不涨就是没效果
- 训练策略改进需要多 epoch 看趋势的前提是**引入了新的梯度回传路径或信号类型**（如 curriculum denoising 改了 denoising 机制），而不是调整已有 loss 的系数
- 用户质疑时先自己检查一遍推理链条，不要急于辩护

### 2026-07-01: 反复"提案→被否定→推翻→再提案"的错误模式

**错误模式**：
在三次"重新审视"请求中，每次都出现同样循环：
1. 我提出一个方案（MAL / FDR+GO-LSD / O2M aux head / SACQ / EASE-DETR / QR-DETR）
2. 用户让我重新评估审视
3. 我立刻找到该方案在 RT-DETR 上的致命缺陷，自己推翻

**原因**：
- 在**提出方案时**没有做足够的自我质疑。想的是"这个看起来能 work"，不是"为什么这个会 fail"
- 被用户要求评估时，才启动批判性思维。但这个批判在提案时就应该完成
- 造成的后果：用户每次都要被动担任"叫停者"，反复纠正我的判断

### 2026-07-03: J2 (频域扰动) 未经充分评估就提案

**错误行为**：
J+L 失败后，我立刻提出 J2（Fourier 域扰动替代高斯噪声），并写了完整代码和配置。
用户让重新评估后，我才发现 FFT 在 content embedding 维度上无理据——256 维 learnable vector 没有频率语义。

**为什么是错的**：
提出方案时想的是"Self-Bootstrapping 用了频域扰动，移植过来就能用"，没有想机制是否成立。
Self-Bootstrapping 扰动的是 ViT 的 spatial patch features（有 2D 空间排列），J2 扰动的是 content embedding（无结构向量），本质不可类比。

### 2026-07-03: ECA backbone 未经五问直接提案

**错误行为**：
用户说"可以尝试修改骨干网络"，我直接抛出 ECA（2020），写了完整代码和2个config。
用户质问"通过五问了吗"，我才承认没有——ECA 是 6 年前的方法，"换了个位置放"不构成方法论贡献。

**根本原因**：
看到任务后直接往最省事的方案上跳（ECA 已经在 attn_modules.py 里写好了），跳过了自检。

### 2026-07-03: Consistency 失败的原因——冗余

J+Consistency 的三个 epoch 完全跑完：epoch 0~2 mAP 与 J-only 无差异。事后分析：
VFL loss 本身就已经把 K=2 组 query 拉向同一个 GT，额外的 consistency loss 是冗余。
**这条管线需要的不是第 11 条梯度路径，是改变已有路径的结构（如 J 的匹配方式）或改变特征本身（如 ISFE/CSCG 的 encoder 增强）。**

---

## 论文创新方法论（来自用户）

**"创新不必从零开始。"**

1. **复现 baseline**：找一篇最新的开源顶会论文，跑通代码，复现结果。
2. **理解模块**：拆解 baseline 的网络（MLP/CNN/Transformer/Mamba 是骨架，其余是核心模块），搞清楚每个模块的作用。
3. **找可替换模块**：在同期其它顶会论文中找**功能相似**的模块，直接替换（缝合）。不需要发明新模块。
4. **写论文时隐藏替换操作**：只要替换后的网络能跑出跟 baseline 相当（或更好）的结果，就可以写。写作框架不是"我们换了 X 为 Y"，而是 baseline 的叙事逻辑——分析领域问题，提出我们的想法。
5. **适配到自己的领域**：如果能根据研究领域对模块做适配（不是原样搬过来），故事就更完整。

**对这个项目的含义**：RT-DETR R18 的每个模块——HybridEncoder 的 CSPRepLayer、FPN/PAN 融合、TransformerEncoder 的位置——都是可替换的靶点。同期论文（D-FINE、Mr. DETR、DEIM、QD-DETR）里都有可缝合的组件。

---

## 提方案前的自检规则（六问）

**用户没要求提新方案时，不主动提新方案。**

如果用户要求提案，必须先通过以下 6 问：

1. **机制成立吗**：核心机制在 RT-DETR 的上下文里是否站得住？（不是"那篇论文做了"——那篇论文的上下文可能完全不同。J2 死在这一点上：Self-Bootstrapping 的 FFT 对 patch features 有意义，对 content embedding 毫无意义。）

2. **已有机制在做同样的事吗**：VFL、aux loss、FPN、DN 是否已经在提供等价信号？（Consistency 死在这一点上：VFL 已经把 K 组 query 拉向同一个 GT。）

3. **组合是否产生新知**：单个已有模块不值一篇论文。但多个模块的组合 + 它们之间特定的协同机制 + 针对 RT-DETR 具体缺口的设计 = 可以。（ECA 单个模块 → 不行。ISFE = inverted bottleneck + DWConv + 特定插入位置 → 行。CSCG = channel gate + cross-scale propagation → 行。）关键是：组合后的协同效应是否超过了各自单独使用的效果？是否存在一个明确的 design rationale 解释为什么这些模块要一起工作？

4. **架构有什么具体阻碍**：通道数、显存、预训练权重、计算瓶颈？具体到代码层面。

5. **如果失败，第一个调试信号是什么**：loss 的值和趋势？某个特定 metric？不能是"跑完 epoch 0 看 mAP"这种笼统答案。必须能在训练过程中就判断。

6. **最短验证周期**：最少几个 epoch 能判断方向对错？能否并行验证？

**没通过这六问的方案禁止提出。**

### 方案设计的原则

- 优先在 encoder/backbone 层面找机会（这里的信息没有被 10 条梯度路径饱和）
- 优先改变**特征本身**或**信号结构**，避免在已有信号上加第 11 条 loss
- 单个已有模块不算创新，但**多个模块的缝合组合 + 特定的协同机制 + 针对 RT-DETR 缺口的设计理由**可以构成新的 idea
- 提方案时必须同时回答"它最容易怎么死"——把这个答案写出来再决定要不要提
